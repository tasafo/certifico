<%= render partial: 'form', layout: 'shared/form', locals: { cols: 6, title: "#{t('title.action.import')} #{t('title.models.subscribers')}<br/>#{@certificate.category.name.capitalize}: #{@certificate.title}", url: certificate_import_subscribers_path(@certificate) } %>
