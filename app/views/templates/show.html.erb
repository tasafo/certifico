<p>
  <strong><%= t('mongoid.attributes.template.name') %>:</strong>
  <%= @template.name %>
</p>

<p>
  <strong><%= t('mongoid.attributes.template.image') %>:</strong>
  <%= cl_image_tag(@template.image, width: 50, height: 50) %>
</p>

<%= link_to t('title.action.edit'), edit_template_path(@template), class: 'btn btn-default' %> <%= link_to t('title.action.back'), templates_path, class: 'btn btn-default' %>
