<%= render partial: 'form', layout: 'shared/form', locals: { cols: 6, title: "#{t('title.action.new2')} #{t('mongoid.models.subscriber')} - #{@certificate.category.name}: #{@certificate.title}", url: certificate_subscribers_path(@certificate) } %>
