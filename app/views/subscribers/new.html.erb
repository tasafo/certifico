<%= render partial: 'form', layout: 'shared/form', locals: { cols: 6, title: "#{t('title.action.new')} #{t('mongoid.models.subscriber')}<br/>#{@certificate.category.name.capitalize}: #{@certificate.title}", url: certificate_subscribers_path(@certificate), form: 'new' } %>
