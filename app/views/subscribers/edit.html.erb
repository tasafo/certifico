<%= render partial: 'form', layout: 'shared/form', locals: { cols: 6, title: "#{t('title.action.editing')} #{t('mongoid.models.subscriber')} <br/> #{@certificate.category.name.capitalize}: #{@certificate.title}<br/>#{t('mongoid.models.user')}: #{@subscriber.user.full_name} (#{@subscriber.user.email}) ", url: certificate_subscriber_path(@certificate), form: 'edit' } %>
