<%= render partial: 'form', layout: 'shared/form', locals: { cols: 6, title: "#{t('title.action.editing')} #{t('mongoid.models.subscriber')} - #{@certificate.category.name}: #{@certificate.title}", url: certificate_subscriber_path(@certificate) } %>
