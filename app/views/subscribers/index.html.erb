<%= render partial: 'index', layout: 'shared/form',
           locals: {
             cols: 12,
             title: "#{t('mongoid.models.certificate')} #{@certificate.category.preposition} #{@certificate.category.name} #{@certificate.title} - #{@subscribers_count} #{t('title.models.subscribers')}"
           } %>
