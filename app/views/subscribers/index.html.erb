<%= render partial: 'index', layout: 'shared/form', locals: { cols: 12,
  title: "#{t('mongoid.models.certificate')}: #{@certificate.title} - #{t('title.models.subscribers')} (#{@certificate.subscribers.count})" } %>
