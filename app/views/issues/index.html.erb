<%= render partial: 'index', layout: 'shared/form', locals: { cols: 12,
  title: "#{@subscribers.size} #{t('title.models.certificates')} #{t('title.certificate.issued')} - #{current_user.full_name}" } %>
