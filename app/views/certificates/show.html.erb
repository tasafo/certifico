<%= render partial: 'show', layout: 'shared/form', locals: { cols: 6, title: t('mongoid.models.certificate') } %>

<%= render partial: 'subscribers/index', layout: 'shared/form', locals: { cols: 12,
  title: "#{t('title.models.subscribers')} (#{@certificate.subscribers.count})" } %>
